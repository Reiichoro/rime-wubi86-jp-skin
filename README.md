
## 📸 視覺預覽 / Preview

![Preview](images/tongwen.webp)

---
# 🌸 Rime Trime: 86 Wubi · Japanese Aesthetic UI Theme
### 「從代碼到美學的跨維度重塑 —— 一場關於輸入體驗的職人實驗」
### "A Cross-Dimensional Reconstruction from Code to Aesthetics — A Craftsman's Experiment in Input Experience"

---

## 🖋️ 開發心路歷程：為何要如此執著？ / The Development Journey

這不僅僅是一個簡單的配色方案，這是一場與 `.yaml` 配置文件的深度博弈。
This is far more than a simple color scheme; it is a deep-seated gamble against `.yaml` configuration files.

在開發這款插件的過程中，我面臨了無數次「技術碰壁」。同文輸入法（Trime）的自定義程度極高，但代價是極其複雜的參數邏輯。為了實現**全界面日語化**，我必須在數千行代碼中，逐一精確定位每一個 UI 元素的屬性。

**我曾遇到的技術障礙 / Technical Hurdles Overcome:**
- **字體渲染失真 (Font Rendering)**：為了呈現完美的日系黑體質感，我不斷調整 `label_font` 與 `candidate_text_size`，經歷了數十次重啟與內存清理。
- **配色衝突 (Color Conflicts)**：處理「鍵盤背景」與「選詞框」的視覺層次時，文字曾多次被背景吞噬。我最終通過對十六進制色值的微調，才達到了「視覺零負擔」的平衡感。
- **佈局形變 (Layout Deformations)**：不同解析度下的按鍵間距常發生不可控位移。我推翻了三套初始方案，才最終確定了這套兼顧 86 五筆與日文美感、且在各類螢幕下都能穩定顯示的佈局。

---

## 🛡️ 核心宣言：絕對的隱私安全 / Absolute Privacy & Security

身為一名技術職人，我深知輸入數據的敏感性。在此鄭重聲明：
As a technical craftsman, I am acutely aware of the sensitivity of input data:

1. **零監控，純在地 (Zero Monitoring)**：本插件僅為 `yaml` 靜態配置文件，**不含任何二進制執行檔，不具備任何聯網權限**。
2. **無後台，綠色純淨 (No Backend)**：這是一個純粹的美化補丁。它不收集、不存儲、不傳送你的任何輸入習慣或隱私數據。
3. **完全開源 (Fully Open Source)**：代碼透明，任何人都可以隨時審閱所有配置細節。

**它是綠色的，它是安全的，它是為了純粹的輸入快感而生的。**
**It is Green. It is Secure. It is born for the pure joy of typing.**

---

---

## 🛠️ 安裝方法 / Installation

1. **備份 (Backup)**：在修改前，請務必備份您原有的 `rime` 文件夾。
2. **解壓詞庫 (Unzip Dictionary) 🚨 重要 / IMPORTANT**:
   > **[ZH]** 由於 GitHub 上傳限制，`japanese.mozc.dict.yaml` 已被壓縮為 `.zip` 格式。**在使用前，請務必先將其解壓**，確保 `rime` 目錄下存在原始的 `.yaml` 檔案，否則日文輸入將無法正常工作。
   >
   > **[EN]** Due to GitHub upload limits, `japanese.mozc.dict.yaml` is compressed as a `.zip` file. **You MUST unzip it before use.** Ensure the raw `.yaml` file is placed in your `rime` directory, or the Japanese input will not function correctly.
3. **覆蓋 (Overwrite)**：將本倉庫中所有解壓後的檔案放入手機儲存空間中的 `rime` 目錄下。
4. **部署 (Deploy)**：打開同文輸入法，點擊「部署（Deploy）」，等待幾秒鐘即可。

---


## 🤝 致謝與交流 / Contact

如果你也跟我一樣，在尋求一種極致的輸入美學，歡迎 **Star** 這個項目或透過我的官網聯繫我。
If you seek the ultimate input aesthetic, feel free to **Star** this project or contact me via my [Official Website](https://reiichoro.github.io).